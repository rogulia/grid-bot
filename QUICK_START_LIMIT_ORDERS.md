# Quick Start: Limit Orders

## 🚀 Быстрый старт

Бот теперь использует **лимитные ордера** для экономии комиссий (0.020% вместо 0.055%).

### Что изменилось?

**Ничего не нужно настраивать!** Лимитные ордера работают автоматически.

### Как это работает?

```
Ордер → Лимитный (10 сек) → Не исполнился? → Retry → Retry → Retry → Market order
                ↓
            Исполнился ✅
        (экономия 64% комиссий)
```

## Тестирование

### 1. Dry Run (рекомендуется сначала)

В `config/config.yaml`:
```yaml
accounts:
  - id: 1
    dry_run: true  # ← Включить
```

Запустить бота и проверить логи:
```bash
tail -f logs/*.log | grep "Limit order"
```

Ожидаемые сообщения:
```
✅ Limit order placed: Buy 1.5 @ $220.0660
✅ Limit order FILLED: Buy 1.5 @ $220.0660
```

### 2. Demo Account

После успешного dry run:
```yaml
accounts:
  - id: 1
    demo_trading: true
    dry_run: false  # ← Выключить
```

Мониторить 24-48 часов.

### 3. Проверка статистики

```bash
# Успешные лимитные ордера
grep "Limit order FILLED" logs/*.log | wc -l

# Фоллбэки на маркет (должно быть <20%)
grep "falling back to MARKET" logs/*.log | wc -l

# Таймауты
grep "Limit order timeout" logs/*.log | wc -l
```

## Настройки (опционально)

Если лимитные ордера плохо исполняются на вашем символе, можно настроить в `config/constants.py`:

```python
# Для высоколиквидных (BTC, ETH):
LIMIT_ORDER_PRICE_OFFSET_PERCENT = 0.02  # Меньше сдвиг

# Для низколиквидных (шиткоины):
LIMIT_ORDER_PRICE_OFFSET_PERCENT = 0.05  # Больше сдвиг
LIMIT_ORDER_TIMEOUT_SEC = 8              # Быстрее фоллбэк
```

## Ожидаемая экономия

| Оборот | Старые комиссии | Новые комиссии | Экономия |
|--------|----------------|----------------|----------|
| $1000/день | $0.55 | $0.27 | $0.28/день |
| $1000/месяц | $16.50 | $8.10 | **$8.40** |

Для 3 аккаунтов: **~$25/месяц экономии**

## Откат на маркет-ордера

Если не понравится, можно вернуться:

В `grid_strategy.py` найти три блока:
```python
# order_id = self.limit_order_manager.place_limit_order(...)
```

Закомментировать и раскомментировать старый код с `Market`.

## Поддержка

**Логи:** `logs/*.log`  
**Документация:** `docs/LIMIT_ORDERS.md`  
**Миграция:** `docs/MIGRATION_LIMIT_ORDERS.md`  
**Детали:** `IMPLEMENTATION_SUMMARY.md`

## ✅ Что гарантировано

- ✅ Ордер **всегда** исполнится (максимум 30 сек задержка)
- ✅ Emergency close использует **маркет** (без задержек)
- ✅ WebSocket отвалился? Таймер все равно сработает
- ✅ Dry run режим работает как раньше

## Целевые метрики

- **Fill rate:** >80% лимитных ордеров должны исполниться
- **Fallback rate:** <20% переходов на маркет
- **Экономия:** ~50% от комиссий

Если метрики хуже - напишите, подстроим параметры под ваши символы.

---

**Вопросы?** См. `docs/LIMIT_ORDERS.md` для подробностей.

